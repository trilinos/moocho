(FRAMEWORK, INSTALL) {
    TESTS = ./MoochoUtilities/test_stop_watch.exe;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = ./IterationPack/TestIterationPack.exe;
    COMM = SERIAL;
}
